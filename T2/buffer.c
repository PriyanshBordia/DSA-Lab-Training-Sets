/*
struct QUEUE
{
	int arr[SIZE];
	int front;
	int rear;
};

void insertLeft(int val)
{
	assert(q. == SIZE - 1)

}

void push(struct QUEUE *q, int val) 
{
	assert(isFull(q))
	{
		printf("Queue is Full\n");
	}
	
	else if ()
	{
		q->rear = q->front = 0;
	}

	q->arr[q->rear] = val;
}

struct QUEUE fill(struct QUEUE q)
{
	int i = 2;
	while (q.rear != SIZE)
	{
		push(i++);
	}

	return q;
}

int peek(struct QUEUE q)
{
	return q.arr[q.front];
}

int isEmpty(struct QUEUE q)
{
	if (q.front == -1)
		return 1;
	else 
		return 0;
}

struct QUEUE init(struct QUEUE q)
{
	q.front = q.rear = -1;
	return q; 
}

struct QUEUE temp(struct QUEUE q)
{
	return q;
}

int size(struct QUEUE q)
{
	if (q.front == -1)
		return 0;
	return (q.rear - q.front);
}
*/